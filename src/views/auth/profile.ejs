<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CP KKU Study Notes</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>

<main class="container">
<h2>โปรไฟล์ของฉัน</h2>
<% if (message){ %><div class="alert"><%= message %></div><% } %>
<form method="post">
  <label>ชื่อผู้ใช้ <input name="username" value="<%= user.username %>"/></label>
  <label>อีเมล <input name="email" value="<%= user.email %>"/></label>
  <label>สาขา/โปรแกรม <input name="program" value="<%= user.program||'' %>"/></label>
  <label>ชั้นปี <input name="year" value="<%= user.year||'' %>"/></label>
  <label>แนะนำตัว <textarea name="bio"><%= user.bio||'' %></textarea></label>
  <button>บันทึก</button>
</form>
</main>
</body>
</html>
